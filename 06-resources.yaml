apiVersion: v1
kind: Pod
metadata:
  name: resource
  namespace: roboshop
  labels:
    project: roboshop
    compontent: frontend 
    envrionment: dev 
spec:
  containers:
  - name: nginx
    image: nginx:1.14.2
    resources:
      requests:
        memory: "64Mi"
        cpu : "250m"
      limits:
        memory: "128Mi"
        cpu: "300m"
