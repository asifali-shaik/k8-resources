apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: frontend
  labels:  # these labels are replicaset labels
    project: expense 
    envrionment: dev 
    tier: frontend
spec:
  # modify replicas according to your case
  replicas: 3
  selector:
    matchLabels:
      project: expense 
      envrionment: dev 
      tier: frontend
  template:
    metadata:
      labels:
        project: expense 
        envrionment: dev
        tier: frontend 
    spec:
      containers:
      - name: nginx
        image : nginx 

